# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.claude

# Local fallback for trader state when Redis env vars aren't set. Must never
# be committed — a stale snapshot here gets deployed fresh on every redeploy,
# silently overwriting the live bot's real trade history.
server/trader-state.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
